// function pet(animal) {
//     console.log(`I have a ${animal} for a pet`);
// }

// pet("cat");

// function petFirst(firstName)
// function petLast(lastName)
// function petFull(firstNamelastName); {
//     console.log(`My pets name is ${firstName} ${lastName}.`);
// }

// petFull(Ginger, Wolf);

function name(lastName, firstName) {
    let fullName = lastName + ", " + firstName;
    console.log(fullName);
    return (fullName);
}

name("Wolf", "Ryan");

